# ─── Wereact Website Factory — API keys ───
# Copy this file to ".env" and fill in your keys, then:  source .env
# (or the factory will read .env automatically via the Asset Engine)

# ─── Asset Engine — image providers ───
# Fallback order (free-first): pexels → unsplash → together → fal → gemini
# Override with IMAGE_PROVIDER_ORDER, e.g. IMAGE_PROVIDER_ORDER=fal,pexels,...

# FREE real photography (commercial-use, self-hostable) — primary sources:
# Pexels — https://www.pexels.com/api/  (create a free API key)
PEXELS_API_KEY=
# Unsplash — https://unsplash.com/oauth/applications (create app → Access Key)
UNSPLASH_ACCESS_KEY=

# AI image generation (need credits/billing) — quality upgrades:
# 1) Google Gemini "Nano Banana" (image gen needs billing enabled on the project)
#    Get a free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# 2) FLUX via fal.ai — photoreal heroes
#    Get a key: https://fal.ai/dashboard/keys
FAL_KEY=

# 3) FLUX via Together AI — last-resort free schnell
#    Get a key: https://api.together.ai/settings/api-keys
TOGETHER_API_KEY=

# higgsfield is used via its MCP connection (no key needed here).

# ─── Optional: SEO Google integrations (claude-seo /seo google) ───
# Google PageSpeed Insights API key (for Core Web Vitals lab data)
# https://developers.google.com/speed/docs/insights/v5/get-started
PAGESPEED_API_KEY=

# secrets
.env
.env.local
*.key

# deps & build
node_modules/
.next/
out/
dist/
.vercel/

# os
.DS_Store

# generated client sites are self-contained (each gets its OWN git + GitHub repo),
# so the factory repo doesn't track them — only the _TEMPLATE skeleton.
clients/*
!clients/_TEMPLATE/
!clients/_TEMPLATE/**
**/.npm-cache/

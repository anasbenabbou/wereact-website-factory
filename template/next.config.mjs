/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Generated assets live in /public; remote patterns can be added per project.
    remotePatterns: [],
  },
};

export default nextConfig;
